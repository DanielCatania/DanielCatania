import styled from "styled-components";

const WavesBox = styled.svg`
  position: absolute;
  bottom: 0;
  right: 0;
  left: 0;

  overflow-x: hidden;
  width: 100vw;

  transform: scaleX(1.2);
`;

export default WavesBox;
